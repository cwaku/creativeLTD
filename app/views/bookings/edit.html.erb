<div class="container mt-5">
  <div class="card">
    <div class="card-header">
      <h1>Edit Booking</h1>
    </div>
    <div class="card-body">
      <%= render 'form', booking: @booking %>
      <%= link_to 'Back to Bookings', bookings_path, class: 'btn btn-secondary mt-3' %>
    </div>
  </div>
</div>
